# Changelog

## 1.0.0 - 2026-08-28

- إصلاح فشل تصدير فيلم كامل عند اختلاف عداد PCS الخام عن عدد صور PGS القابلة للرسم بإطار واحد.
- تصدير ملف `.partial` قابل للاسترجاع عند فشل بعض الإطارات بدل حجب كل النص المنجز.
- إضافة قائمة انتظار ملفات دائمة وتشغيل متتابع مع استعادة المهام بعد إعادة فتح التطبيق.
- استئناف المهمة تلقائيًا بعد انقطاع خادم Vision مع الاحتفاظ بالإطارات المكتملة.
- إعادة OCR تلقائيًا بمعالجة طبيعية وتباين رمادي وقناع ثنائي قبل تسجيل فشل الإطار.
- إضافة فك DVB Subtitle وXSUB عبر PyAV مع اختبار تكامل على عينات FFmpeg حقيقية.
- إضافة قراءة مجلد VIDEO_TS وملفات IFO/VOB واختيار عنوان DVD عبر FFmpeg dvdvideo.
- إزالة ملف تشغيل المحرك والتشغيل التلقائي؛ أصبح التطبيق عميل API فقط والمحرك يشغّله المستخدم.
- تبسيط فحص الجاهزية ليختبر اتصال الخادم ويجلب الموديلات دون اكتشاف أو تشغيل برامج خارجية.
- إزالة صفحة المراجعة والجودة وصفحة فحص النظام والشريط الجانبي واعتماد واجهة RTL من صفحة واحدة.
- دمج اختيار المحرك والموديل وفحص الاتصال في تدفق مختصر داخل شاشة التحويل.
- دمج فحص الخادم وجلب موديلات Vision في زر واحد يعمل في الخلفية.
- نقل عنوان API ومفتاحه والمهلة إلى نافذة إعدادات اتصال صغيرة.
- إضافة سحب وإفلات واختيار عدة ملفات وتحليلها وتشغيلها تلقائيًا عبر قائمة انتظار دائمة.
- إضافة اختيار رقم عنوان DVD بدل افتراض العنوان الأول.
- عرض عدد الصور والأسطر والوقت المنقضي والمتبقي ومسار النتيجة وزر فتح مجلد الحفظ.
- إخفاء سجل المعالجة وقائمة الانتظار عند عدم الحاجة لتقليل ازدحام الواجهة.
- إضافة اختبار OCR لأول ثلاث صور قبل متابعة المشروع الطويل مع تقدير مدة المعالجة.
- تحديث ملف الترجمة الجزئي ذريًا كل 25 نتيجة جديدة وحذفه بعد نجاح الملف النهائي.
- إضافة مدقق توقيت نهائي وتقرير لعدد الصور والأسطر والمدة وحالة التوقيت.
- تحسين حفظ الإعدادات الذري على Windows ورسائل الخطأ عند تعذر الكتابة.
- إضافة حركة تحميل مختصرة أثناء فحص الخادم وقراءة الموديلات دون تجميد الواجهة.
- تحسين تعليمات OCR للنصوص العربية وRTL للحفاظ على ترتيب Unicode المنطقي للكلمات.
- الحفاظ على توقيت PGS الأصلي عند استخراجه من MKV/MKS بدل تصفير أول ترجمة.
- التحقق من أول وآخر PTS بعد الاستخراج وإيقاف المهمة قبل OCR إذا اكتُشف أي انزياح.
- تغيير هوية استئناف مشاريع PGS لمنع إعادة استخدام توقيتات مخزنة بالطريقة القديمة.
- منع إسقاط ترجمة مرئية بصمت عندما يعيد الموديل `EMPTY`، واعتبار الإطار فاشلًا.
- تحسين صور PGS الصغيرة بالتحجيم المحافظ والتباين والحدة، ورفع جودة JPEG دون خفض ألوان النص.
- تمرير لغة المسار إلى تعليمات OCR كتلميح تعرف فقط مع منع الترجمة وإعادة الصياغة.
- حساب إجمالي أحداث PGS مسبقًا لتحسين نسبة التقدم والوقت المتوقع.
- ترقية مخطط SQLite تلقائيًا مع الحفاظ على مشاريع المستخدم السابقة.
- دعم استخراج VobSub المضمّن داخل MKV/MKS إلى IDX/SUB عبر MKVToolNix.
- التحقق من أول وآخر توقيت VobSub قبل OCR وحذف الاستخراج المؤقت عند اكتشاف أي انزياح.
- تصحيح الإزاحة الثابتة فقط عندما يثبت تطابق الفرق عند أول وآخر توقيت، ورفض أي انزياح غير ثابت.
- إضافة اكتشاف تلقائي لـmkvmerge وmkvextract وعرض جاهزيتهما دون ازدحام الواجهة بالمسارات.
- إضافة دعم Blu-ray BDN XML وصور PNG مع Timecode أصلي وتجميع الرسومات المتعددة.
- دعم امتداد PGS المستقل وإضافة M2TS/MTS/TS إلى مصادر الاختيار.
- منع تصدير المشروع إذا اختلف عدد أحداث المصدر عن عدد الإطارات التي قرأها المحلل.
- تصنيف MicroDVD وSubViewer النصيين بصورة صحيحة بدل معاملتهما كـVobSub صوري.
- تضمين ملف VobSub المرافق وصور BDN في بصمة المشروع لضمان استئناف صحيح.
- اكتشاف DVB/XSUB كترجمات صورية وإيقافها برسالة واضحة بدل تمريرها لمسار خاطئ.
- توسيع الاختبارات إلى 70 اختبارًا تشمل الصيغ الجديدة واكتمال الإطارات والتوقيت والواجهة المبسطة.
